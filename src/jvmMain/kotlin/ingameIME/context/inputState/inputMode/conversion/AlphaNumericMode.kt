package ingameIME.context.inputState.inputMode.conversion

import ingameIME.context.IInputContext

object AlphaNumericMode : IAlphaNumericMode {
    external override fun onApplyState(context: IInputContext)
}